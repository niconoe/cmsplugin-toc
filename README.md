This is a simple django CMS plugin that generates a simple table of contents (nested list of titles) based on the content of a text plugin instance.

It will generate anchor links if the titles have ids.